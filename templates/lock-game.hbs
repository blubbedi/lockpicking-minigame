<div class="lp-game-root">
  <header class="lp-header">
    <h2>Schlossknacken</h2>
    <div class="lp-subtitle">
      <span>{{actorName}}</span>
      <span>– DC {{dc}}, Bonus {{bonus}}{{#if disadvantage}}, mit Nachteil{{/if}}</span>
    </div>
  </header>

  <section class="lp-instructions">
    <p>
      Drücke nacheinander die angezeigten <b>Pfeiltasten</b>, bevor der Zeitbalken leer ist.
      Jede Taste wird einzeln abgefragt. Geschaffte Schritte werden grün markiert.
    </p>
  </section>

  <section class="lp-steps-section">
    <div class="lp-steps-label">Gesamte Sequenz:</div>
    <div class="lp-steps">
      {{#each steps}}
        <div class="lp-step pending" data-step="{{index}}">
          <span class="lp-step-label">{{label}}</span>
        </div>
      {{/each}}
    </div>
  </section>

  <section class="lp-qte-section">
    <div class="lp-timebar-outer">
      <div class="lp-timebar-inner"></div>
    </div>

    <div class="lp-qte-key-display">
      <div class="lp-qte-key-box">
        <span class="lp-qte-key-label">Bereit?</span>
      </div>
      <div class="lp-qte-key-hint">
        Aktuelle Taste
      </div>
    </div>
  </section>

  <footer class="lp-footer">
    <button type="button" class="lp-btn lp-btn-primary" data-action="start">
      Starten
    </button>
    <button type="button" class="lp-btn" data-action="close">
      Abbrechen
    </button>
  </footer>
</div>
