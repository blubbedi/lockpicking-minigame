<div class="lp-game">
  <p><strong>{{actor.name}}</strong> versucht, ein Schloss zu knacken.</p>
  <p>DC: <strong>{{dc}}</strong>, Bonus: <strong>{{#if (gte bonus 0)}}+{{/if}}{{bonus}}</strong></p>

  <div class="lp-bar-container">
    <div class="lp-bar">
      <div class="lp-bar-fill"></div>
    </div>
    <p>Starte die Bewegung und stoppe, wenn der Balken im unsichtbaren Zielbereich ist.</p>
  </div>

  <div class="lp-buttons">
    <button type="button" data-action="start">Bewegung starten</button>
    <button type="button" data-action="stop">Jetzt knacken!</button>
  </div>
</div>
