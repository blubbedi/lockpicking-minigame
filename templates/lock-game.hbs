<div class="lp-game-root">
  <header class="lp-header">
    <h2>Schlossknacken</h2>
    <div class="lp-subtitle">
      <span>{{actorName}}</span>
      <span>– DC {{dc}}, Bonus {{bonus}}{{#if disadvantage}}, mit Nachteil{{/if}}</span>
    </div>
  </header>

  <section class="lp-instructions">
    <p>
      Drücke die angezeigte <b>Pfeiltaste</b>, bevor der Zeitbalken leer ist.
      Du musst alle Eingaben nacheinander korrekt treffen, um das Schloss zu knacken.
    </p>
  </section>

  <section class="lp-steps-section">
    <div class="lp-steps-label">Benötigte Eingaben:</div>
    <div class="lp-steps">
      {{#each steps}}
        <div class="lp-step pending"></div>
      {{/each}}
    </div>
  </section>

  <section class="lp-qte-section">
    <div class="lp-timebar-outer">
      <div class="lp-timebar-inner"></div>
    </div>

    <div class="lp-qte-key-display">
      <span class="lp-qte-key-label">Bereit?</span>
    </div>
  </section>

  <footer class="lp-footer">
    <button type="button" class="lp-btn lp-btn-primary" data-action="start">
      Starten
    </button>
    <button type="button" class="lp-btn" data-action="close">
      Abbrechen
    </button>
  </footer>
</div>
